package rpaint

object Settings {
    const val serverPort = 8080
    const val serverHost = "127.0.0.1"

    const val windowWidth = 600
    const val windowHeight = 600

    const val waitForThreadStopMillis: Long = 3000
}